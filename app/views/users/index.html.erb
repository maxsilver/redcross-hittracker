<h4>Users</h4>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.email, edit_user_path(user) %>
      <%= link_to "Delete", user, :method => "delete", :confirm => "Are you sure?" %>
    </li>
  <% end %>
</ul>
