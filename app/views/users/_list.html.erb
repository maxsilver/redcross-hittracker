<ul>
  <% users.each do |user| %>
    <li>
      <%= user.email %>
      <%= link_to "Delete", user, :method => "delete" %>
      <%= link_to "Edit", edit_user_path(user.id) %> 
    </li>
  <% end %>
</ul>
